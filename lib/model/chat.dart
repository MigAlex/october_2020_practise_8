import 'package:flutter/material.dart';

class Chat {
  final String urlAvatar, username, message;

  const Chat({
    @required this.urlAvatar,
    @required this.username,
    @required this.message,
  });
}
